# ESLint Config

Modern ESLint 9+ configuration with Flat Config support for JavaScript, TypeScript and Vue 3.

## ✨ Features

- 🔥 **ESLint 9+** with new Flat Config format
- 📦 **JavaScript/TypeScript** support with latest syntax
- 🖖 **Vue 3** support (with TypeScript in SFC)

## 👀 How to use?

eslint.config.js:
```bash
import ESLintConfig from 'wasdwave-eslint'

export default [...ESLintConfig]
```

## 📦 Installation

```bash
npm i wasdwave-eslint
```
